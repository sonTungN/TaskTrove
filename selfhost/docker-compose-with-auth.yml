services:
  tasktrove:
    image: ghcr.io/dohsimpson/tasktrove:latest
    container_name: tasktrove
    ports:
      - "3456:3000"
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
    environment:
      - AUTH_SECRET=CHANGE_ME
    restart: always
